# Copy to .env and fill in. Loaded from the repo root by rescheck/llm.py.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Raises the GitHub API limit from 60 to 5000 requests/hour when the
# grader inspects the repositories linked from your resume.
# GITHUB_TOKEN=ghp_...
